import React from 'react'
import ReactDOM from "react-dom"
import App from "./components/App"




const container = document.getElementById('tag')

const tag = ReactDOM.createRoot(container)

tag.render(<App />)

